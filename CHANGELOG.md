# Changelog

## 0.3.0 - 2026-08-13

- **目录模式**:`luanma 乱码目录/` 预览重命名计划,`--rename` 把已经
  解压出来的乱码文件名原地改回正确名字(自底向上处理嵌套目录,
  重名自动加 ` (2)` 后缀,预览与实际执行结果一致)
- 目录模式安全护栏:逐名合理性打分,`café.txt` 这类 cp437 可编码的
  真欧文文件名不会被误改;默认演习(dry-run),不加 `--rename` 不动盘
- 安全修复:预览输出中和文件名里的控制字符,恶意压缩包无法向终端
  注入 ANSI 转义序列
- 修复:UNC 网络路径(`\\server\share`)的长路径前缀改用
  `\\?\UNC\` 正确形式
- 修复:目录条目不再因带加密标志而误报"需要密码"
- 修复:`--fix` 时目录条目强制以 STORED 方式写入;逐条目注释
  (entry comment)现予保留
- CLI:`--help` 增加中文示例;glob 支持 `**` 递归;检测置信度阈值
  微调(混合 ASCII 的中文名不再轻易降级);支持 `python -m luanma` 调用
- 测试增至 78 个

## 0.2.0 - 2026-08-13

- 加密压缩包支持:`-p` 密码解压 ZipCrypto 加密条目;`--fix` 可解密转存;
  未提供密码时逐条明确提示而不是中途崩溃
- Windows 长路径支持:目标路径超过 260 字符自动使用 `\\?\` 前缀,
  深层中文目录树不再解压失败
- 通配符自我展开:PowerShell/cmd 不展开 `*.zip`,现在 CLI 自己处理
- 修复:压缩包内"文件"与"同名目录"冲突时不再抛异常,记录为条目级错误
- 修复:`--fix` 重名条目的 ` (2)` 后缀处理规范化(正确保留扩展名)
- 检测:GBK 升级为 GB18030(超集,覆盖生僻字与 4 字节序列),
  识别 CJK 扩展 A 区字符
- CLI:`-d`/`-o` 参数校验;部分条目失败时退出码为 1(全部成功 0,
  无法打开 2)
- 测试增至 58 个,新增手写 ZipCrypto 加密器构造真实加密样本

## 0.1.0 - 2026-08-13

首个版本。

- 文件名编码自动检测:GBK / Big5 / Shift-JIS / EUC-KR / 漏标 flag 的 UTF-8,
  基于高频汉字表(简繁约 900 字)与高频韩文音节表打分,附置信度与候选排序
- 三种动作:预览对照表(默认,只读)、按检测编码安全解压(`-x`)、
  转存为标准 UTF-8 压缩包(`--fix`,设置 flag bit 11)
- 安全防护:zip-slip 路径穿越防护、Windows 非法字符与保留设备名清洗、
  重名自动加后缀、源压缩包永不修改
- 自动跳过 `__MACOSX/`、`._*`、`.DS_Store`、`Thumbs.db` 等系统垃圾文件
  (`--keep-junk` 保留)
- 混合压缩包支持:已标 UTF-8 flag 的条目原样保留,只修未标记的条目
- `--json` 机器可读输出;`-e` 手动指定编码;批量处理多个压缩包
- 保留压缩方式、时间戳、权限位与压缩包注释
